# 🚀 Руководство по развертыванию AutoSphere на Vercel

## Содержание
1. [Предварительная подготовка](#предварительная-подготовка)
2. [Настройка Firebase](#настройка-firebase)
3. [Настройка Vercel Blob Storage](#настройка-vercel-blob-storage)
4. [Настройка переменных окружения](#настройка-переменных-окружения)
5. [Развертывание на Vercel](#развертывание-на-vercel)
6. [Проверка после развертывания](#проверка-после-развертывания)
7. [Устранение неполадок](#устранение-неполадок)

---

## Предварительная подготовка

### Требования
- ✅ Аккаунт Firebase (https://console.firebase.google.com)
- ✅ Аккаунт Vercel (https://vercel.com)
- ✅ Node.js 18+ установлен локально
- ✅ Git установлен локально

### Локальная проверка
```bash
# Клонировать репозиторий
git clone <your-repo-url>
cd studio

# Установить зависимости
npm install

# Создать .env.local из примера
cp .env.example .env.local

# После настройки .env.local, запустить локально
npm run dev
```

---

## Настройка Firebase

### 1. Создание проекта Firebase

1. Откройте [Firebase Console](https://console.firebase.google.com)
2. Нажмите "Add project" (Добавить проект)
3. Введите название проекта (например, "autosphere-prod")
4. Отключите Google Analytics (можно включить позже)
5. Нажмите "Create project"

### 2. Включение Authentication

1. В боковом меню выберите **Build** → **Authentication**
2. Нажмите "Get started"
3. Включите провайдеров:
   - **Email/Password** - обязательно
   - **Google** - рекомендуется
   - Другие по необходимости

### 3. Создание Firestore Database

1. В боковом меню выберите **Build** → **Firestore Database**
2. Нажмите "Create database"
3. Выберите режим **Production mode** (с правилами безопасности)
4. Выберите регион близкий к вашей аудитории (например, europe-west1)
5. Нажмите "Enable"

### 4. Развертывание Security Rules

```bash
# Установите Firebase CLI если еще не установлен
npm install -g firebase-tools

# Войдите в аккаунт
firebase login

# Инициализируйте Firebase в проекте
firebase init

# Выберите:
# - Firestore
# - Storage (если планируете использовать)

# Разверните правила
firebase deploy --only firestore:rules
firebase deploy --only storage:rules
```

**Важно:** Файлы `firestore.rules` и `storage.rules` уже есть в проекте.

### 5. Создание индексов Firestore

```bash
# Разверните индексы
firebase deploy --only firestore:indexes
```

**Примечание:** Создание индексов может занять 5-10 минут.

### 6. Получение Firebase конфигурации

#### Публичная конфигурация (для клиента)

1. Откройте **Project Settings** (шестеренка рядом с "Project Overview")
2. Пролистайте до раздела "Your apps"
3. Нажмите на иконку веб-приложения `</>`
4. Зарегистрируйте приложение (например, "AutoSphere Web")
5. Скопируйте значения из `firebaseConfig`:
   - `apiKey` → `NEXT_PUBLIC_FIREBASE_API_KEY`
   - `authDomain` → `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN`
   - `projectId` → `NEXT_PUBLIC_FIREBASE_PROJECT_ID`
   - `storageBucket` → `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET`
   - `messagingSenderId` → `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID`
   - `appId` → `NEXT_PUBLIC_FIREBASE_APP_ID`

#### Приватная конфигурация (для сервера)

1. Откройте **Project Settings** → **Service Accounts**
2. Нажмите "Generate new private key"
3. Подтвердите и скачайте JSON файл
4. **ВАЖНО:** Этот файл содержит секретные ключи - НИКОГДА не коммитьте его в Git!

Преобразуйте JSON в одну строку для переменной окружения:
```bash
# Метод 1: Вручную скопируйте весь JSON как одну строку без переносов
# Метод 2: Используйте команду
cat path/to/serviceAccountKey.json | tr -d '\n' | tr -d ' '
```

---

## Настройка Vercel Blob Storage

### 1. Создание Blob Store

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Перейдите в раздел **Storage**
3. Нажмите "Create Database" → выберите **Blob**
4. Введите название (например, "autosphere-images")
5. Выберите регион (тот же что и у Firebase)
6. Нажмите "Create"

### 2. Получение токена доступа

1. Откройте созданный Blob Store
2. Перейдите на вкладку **Settings**
3. Найдите "Read-Write Token"
4. Скопируйте значение токена → `BLOB_READ_WRITE_TOKEN`

---

## Настройка переменных окружения

### Для локальной разработки

Создайте файл `.env.local` в корне проекта:

```env
# Firebase Client (Public)
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSy...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abc...
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-ABC123

# Firebase Admin (Server-side - SECRET!)
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}

# Vercel Blob Storage
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...

# Optional: Google Genkit AI
GOOGLE_GENAI_API_KEY=AIzaSy...
```

### Для Vercel (Production)

1. Откройте ваш проект на Vercel
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте все переменные из `.env.local`
4. **Важно:** Для `FIREBASE_SERVICE_ACCOUNT_KEY` используйте тип **Secret**

#### Добавление переменных через CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Добавьте переменные
vercel env add NEXT_PUBLIC_FIREBASE_API_KEY
vercel env add FIREBASE_SERVICE_ACCOUNT_KEY
vercel env add BLOB_READ_WRITE_TOKEN
# ... и так далее для всех переменных
```

---

## Развертывание на Vercel

### Метод 1: Через GitHub (Рекомендуется)

1. Запушьте код в GitHub репозиторий
2. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
3. Нажмите "Add New" → "Project"
4. Выберите ваш GitHub репозиторий
5. Настройте:
   - **Framework Preset**: Next.js
   - **Root Directory**: `./`
   - **Build Command**: `npm run build`
   - **Output Directory**: `.next`
6. Добавьте переменные окружения (см. раздел выше)
7. Нажмите "Deploy"

### Метод 2: Через Vercel CLI

```bash
# Установите Vercel CLI если еще не установлен
npm i -g vercel

# Войдите
vercel login

# Первый деплой (настройка проекта)
vercel

# Продакшн деплой
vercel --prod
```

---

## Проверка после развертывания

### ✅ Чек-лист проверки

#### 1. Базовая функциональность
- [ ] Сайт открывается без ошибок
- [ ] Главная страница загружается
- [ ] Стили применяются корректно
- [ ] Нет ошибок в консоли браузера

#### 2. Аутентификация
- [ ] Форма входа/регистрации открывается
- [ ] Регистрация нового пользователя работает
- [ ] Вход существующего пользователя работает
- [ ] Выход из аккаунта работает
- [ ] Профиль пользователя отображается

#### 3. Firestore
- [ ] Посты загружаются на главной странице
- [ ] Можно создать новый пост
- [ ] Можно отредактировать пост
- [ ] Можно удалить пост
- [ ] Лайки работают
- [ ] Комментарии работают

#### 4. Загрузка изображений
- [ ] Можно загрузить фото профиля
- [ ] Можно загрузить изображение в пост
- [ ] Можно загрузить фото автомобиля
- [ ] Изображения отображаются корректно
- [ ] Next.js Image optimization работает

#### 5. Производительность
- [ ] Lighthouse Score > 80 (Performance)
- [ ] First Contentful Paint < 2s
- [ ] Time to Interactive < 4s
- [ ] No layout shifts (CLS близко к 0)

### Команды для проверки

```bash
# Проверить логи Vercel
vercel logs <deployment-url>

# Локально проверить production build
npm run build
npm run start

# Проверить переменные окружения
vercel env ls
```

---

## Устранение неполадок

### Проблема: "Firebase is not initialized"

**Причина:** Не настроены публичные переменные Firebase

**Решение:**
1. Проверьте что все `NEXT_PUBLIC_FIREBASE_*` переменные добавлены в Vercel
2. Убедитесь что нет опечаток в именах переменных
3. Пересоберите проект в Vercel

### Проблема: "Permission denied" при работе с Firestore

**Причина:** Не развернуты Security Rules или неправильные правила

**Решение:**
```bash
# Проверьте файл firestore.rules
cat firestore.rules

# Разверните правила снова
firebase deploy --only firestore:rules

# Проверьте в Firebase Console → Firestore → Rules
```

### Проблема: "Upload failed" при загрузке изображений

**Причина:** Не настроен Vercel Blob или неверный токен

**Решение:**
1. Проверьте что `BLOB_READ_WRITE_TOKEN` добавлен в Vercel
2. Проверьте что токен активен в Vercel Dashboard → Storage
3. Проверьте логи API route: `/api/upload`

### Проблема: Build fails с TypeScript errors

**Причина:** В коде есть ошибки типов

**Решение:**
```bash
# Локально проверьте типы
npm run typecheck

# Если нужно временно отключить строгую проверку (НЕ рекомендуется для продакшена):
# В next.config.ts добавьте:
typescript: {
  ignoreBuildErrors: true
}
```

### Проблема: "Module not found" errors

**Причина:** Не все зависимости установлены

**Решение:**
```bash
# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Переустановите
npm install

# Проверьте что package.json содержит все нужные пакеты
```

### Проблема: Images не оптимизируются

**Причина:** Неправильная конфигурация Next.js Image

**Решение:**
Проверьте `next.config.ts`:
```typescript
images: {
  remotePatterns: [
    {
      protocol: 'https',
      hostname: '*.public.blob.vercel-storage.com',
      pathname: '/**',
    }
  ],
}
```

### Проблема: "Too many requests" от Firebase

**Причина:** Превышены квоты бесплатного плана

**Решение:**
1. Проверьте использование в Firebase Console → Usage
2. Оптимизируйте запросы (используйте кэширование)
3. Рассмотрите переход на Blaze plan

---

## Рекомендации по безопасности

### ✅ Обязательно

1. **Никогда** не коммитьте `.env.local` в Git
2. Используйте **Secret** тип для чувствительных переменных в Vercel
3. Регулярно обновляйте Firebase Security Rules
4. Включите 2FA для Firebase и Vercel аккаунтов
5. Ограничьте API keys в Firebase Console (по доменам)

### 🔒 Настройка API restrictions

1. Firebase Console → Project Settings → General
2. Найдите API keys
3. Для каждого ключа настройте:
   - **HTTP referrers**: `https://your-domain.vercel.app/*`
   - **IP addresses**: если знаете

---

## Мониторинг и аналитика

### Vercel Analytics

```bash
# Установите пакет
npm install @vercel/analytics

# Добавьте в app/layout.tsx
import { Analytics } from '@vercel/analytics/react'

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
```

### Firebase Performance Monitoring

1. Firebase Console → Performance
2. Следуйте инструкциям для веб-приложений
3. Добавьте SDK в проект

---

## Полезные ссылки

| Ресурс | URL |
|--------|-----|
| Vercel Dashboard | https://vercel.com/dashboard |
| Firebase Console | https://console.firebase.google.com |
| Next.js Docs | https://nextjs.org/docs |
| Vercel Blob Docs | https://vercel.com/docs/storage/vercel-blob |
| Firebase Web Docs | https://firebase.google.com/docs/web |

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи в Vercel Dashboard
2. Проверьте консоль браузера (F12)
3. Проверьте Firebase Console → Functions/Firestore logs
4. Создайте issue в GitHub репозитории

---

**Дата обновления:** Декабрь 2025  
**Версия:** 1.0.0
