# ✅ Технический аудит и дорожная карта улучшений

Этот документ содержит результаты аудита и план по оптимизации, безопасности и масштабированию проекта "AutoSphere" на платформе Vercel.

---

## 🚀 Что стоит проверить сразу

| Задача                          | Как это сделать                                                                                                                              | Почему важно                                                  |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| **Проверить сборку**            | Запустить `npm run build` и убедиться, что нет ошибок или критических предупреждений.                                                       | Без успешной сборки проект не развернётся.                   |
| **Тестировать API‑эндпойнты**   | Запустить `npm run dev` и проверить работу всех серверных эндпойнтов (если они есть, например, в `/pages/api/*`).                          | Ошибки в serverless-функциях могут проявляться только в продакшене. |
| **Проверить переменные окружения** | В файле `.env.local.example` (или аналоге) указать, какие переменные необходимы для работы проекта (e.g., `NEXT_PUBLIC_*`, `FIREBASE_*`). | Отсутствие переменной → ошибки на клиенте/сервере.           |

---

## 🛠️ Что можно улучшить

### 1. Переписать API‑routes в Edge Functions

Vercel Edge Functions выполняются ближе к пользователю (на CDN), что делает их быстрее и дешевле для простых операций.

-   **Сейчас:** Обычные серверные (Node.js) функции, если они есть.
-   **Как сделать:** Для легковесных API, не требующих тяжелых Node.js-зависимостей, можно создать функции в `api/`. Next.js 15 с App Router по умолчанию поддерживает это. Для более сложных случаев, где нужна Node.js, текущий подход остается верным.

**Плюсы:**
-   ~30% быстрее отклик для простых запросов.
-   Снижение затрат на выполнение.

### 2. Снижение размера бандла

| Что                          | Как                                                                                                                            |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| **Lazy-load компоненты**     | Использовать `next/dynamic` для "тяжелых" компонентов, которые не видны на первом экране.                                       |
| **Tree shaking**             | Проверять импорты из библиотек. Например, `import { debounce } from 'lodash-es';` вместо `import debounce from 'lodash';`.       |
| **Проверить `tsconfig.json`**  | Убедиться, что `target` и `module` настроены на современные стандарты (`esnext`), что уже сделано.                               |
| **Анализ бандла**            | Использовать `@next/bundle-analyzer` для визуального анализа того, что занимает больше всего места в сборке.                    |

### 3. SEO & Performance

| Точка                       | Рекомендация                                                                                                                   |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| **Динамические Meta‑теги**  | Для динамических страниц (профили, посты) использовать функцию `generateMetadata` для генерации уникальных `title` и `description`. |
| **next/image**              | Убедиться, что все изображения, загружаемые с внешних источников, используют компонент `<Image>` с указанием `width` и `height`.  |
| **Server Components (RSC)** | Максимально переносить логику получения данных на серверные компоненты, как уже сделано для страниц `/car/[id]` и `/profile/[id]`. |
| **Lighthouse**              | Регулярно запускать аудит в Chrome DevTools для отслеживания производительности, доступности и SEO.                               |

### 4. Безопасность & Переменные окружения

| Что                | Как                                                                                                                                                              |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **API-ключи в `.env`** | Секретные ключи (для Admin SDK) **никогда** не должны начинаться с `NEXT_PUBLIC_`. В проекте это правило соблюдается (`FIREBASE_SERVICE_ACCOUNT_KEY`).      |
| **CORS**           | CORS-заголовки для Storage уже настроены. Для API-роутов в Next.js они по умолчанию защищены (Same-origin). Менять только при необходимости.                  |
| **Security Rules** | Регулярно пересматривать `firestore.rules` и `storage.rules` при добавлении нового функционала, чтобы избежать случайного открытия доступа к данным.              |

### 5. CI/CD & Тестирование

Добавить workflow в `.github/workflows/ci.yml` для автоматических проверок при каждом push.

```yaml
name: CI/CD Pipeline
on: [push, pull_request]
jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Dependencies
        run: npm ci
      - name: Lint Check
        run: npm run lint
      - name: Type Check
        run: npm run typecheck
      - name: Build Project
        run: npm run build
```

---

## 📦 Как это реализовать (пошагово)

#### Lazy-load для тяжелых компонентов:

```tsx
import dynamic from 'next/dynamic';

const HeavyComponent = dynamic(() => import('../components/Heavy'), {
  loading: () => <p>Загрузка...</p>,
  ssr: false, // Отключаем рендеринг на сервере, если компонент чисто клиентский
});
```

#### Оптимизация изображений (пример):
```tsx
import Image from 'next/image';

// Замена <img> на <Image>
<Image
  src="/images/logo.png" // Путь от /public
  width={200}
  height={100}
  alt="Логотип"
  priority // Если изображение в первой видимой области
/>
```

#### CI/CD:
1.  Создать директорию `.github/workflows/` в корне проекта.
2.  Создать файл `ci.yml` внутри этой директории.
3.  Скопировать в него приведенный выше YAML-код.
4.  Запушить в репозиторий. GitHub Actions автоматически подхватит этот workflow.

---

## 🎉 Что будет после изменений

| Фича                             | Ожидаемый эффект                                                              |
| -------------------------------- | ----------------------------------------------------------------------------- |
| **Оптимизация бандла**           | Снижение размера клиентского JavaScript на 15–25%, улучшение метрики LCP.     |
| **Улучшение SEO**                | Лучшее индексирование динамических страниц поисковиками, рост органического трафика. |
| **CI/CD**                        | Меньше ошибок при деплое, автоматическая проверка качества кода при каждом коммите. |
| **Улучшение безопасности**       | Снижение риска утечки секретных ключей и несанкционированного доступа к данным. |

---

## 🎯 Что дальше
1.  Применить вышеуказанные изменения в ветке `dev`.
2.  Открыть Pull Request в `main` и дождаться прохождения CI/CD проверок.
3.  После успешных проверок выполнить слияние, после чего Vercel автоматически развернет обновленную версию.
