
# Пошаговый «Промпт» (шаблон) для настройки проекта в Firebase
(для тех, кто только начинает работать с Firebase и хочет быстро сконфигурировать все нужные сервисы)

## Что это?

Это набор вопросов‑ответов, которые вы будете задавать себе (или коллеге) во время работы с консолью Firebase. Он охватывает: создание проекта, подключение к приложению, аутентификацию, базу данных Firestore, функции Cloud Functions, хранилище Storage и аналитику.

## 1️⃣ Создание проекта в Firebase
| Шаг | Что делать | Ключевые вопросы |
|-----|------------|------------------|
| 1.1 | Перейти на https://console.firebase.google.com/ и нажать Add project | Как называется проект? (например, auto-community) |
| 1.2 | Ввести название проекта и/или ID проекта | Уникальный идентификатор (auto-community-001)? |
| 1.3 | Выбрать регион для облака (оптимально ближе к основной аудитории) | Где находится ваша основная пользовательская база? |
| 1.4 | Отключить/включить Google Analytics (рекомендовано включить, чтобы иметь возможность анализировать поведение пользователей) | Нужно ли вам аналитика? |

**Важно:** После создания проекта вы увидите панель с доступными сервисами.

## 2️⃣ Подключение к приложению

### Для Web‑приложения (React/Next.js)
| Шаг | Что делать |
|-----|------------|
| 2.1 | В консоли Firebase выберите Project settings → вкладка General → Your apps → Web app. |
| 2.2 | Добавьте название, например AutoCommunity Web. |
| 2.3 | Получите конфигурацию (код вида firebaseConfig). |
| 2.4 | Установите SDK:  
```bash
npm i firebase@latest
``` |
| 2.5 | Инициализируйте Firebase в вашем проекте (`src/firebase.js`):  
```js
import { initializeApp } from 'firebase/app';
const firebaseConfig = {/* ваш config */};
export const app = initializeApp(firebaseConfig);
``` |

### Для мобильного (iOS/Android)
Следуйте инструкциям в консоли, скачайте `google-services.json` / `GoogleService-Info.plist`, добавьте их в проект и выполните настройку SDK (Gradle/Maven или CocoaPods).

## 3️⃣ Аутентификация
| Шаг | Что делать |
|-----|------------|
| 3.1 | В консоли Firebase → Authentication → Sign-in method. |
| 3.2 | Активировать нужные провайдеры: <ul><li>Email/Password</li><li>Google (OAuth)</li><li>GitHub (если нужно)</li><li>Phone (для SMS‑автент).</li></ul> |
| 3.3 | Включить Email templates и настроить подтверждение email. |
| 3.4 | Создать правила доступа для Firestore, если понадобится (см. раздел «Правила безопасности»). |

## 4️⃣ Cloud Firestore
| Шаг | Что делать |
|-----|------------|
| 4.1 | В консоли Firebase → Firestore Database → Create database. |
| 4.2 | Выбрать режим: Production (только с правилами) или Test. |
| 4.3 | Настроить регион (тот же, что в проекте). |
| 4.4 | Создать коллекции и документы вручную либо через SDK (firebase.firestore()). |
| 4.5 | Определить правила безопасности:  
```rules
service cloud.firestore {
  match /databases/{database}/documents {
    // Доступ только аутентифицированным пользователям
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
``` |
| 4.6 | Добавить индексы (если нужно) → Indexes → Create index. |

## 5️⃣ Cloud Functions (Node.js)
| Шаг | Что делать |
|-----|------------|
| 5.1 | В консоли Firebase → Functions → Get started. |
| 5.2 | Инициализировать SDK в корне проекта:  
```bash
firebase init functions
```
Выберите Node.js (текущая LTS, например v20). |
| 5.3 | В `functions/index.ts` написать функции:  
```ts
import * as functions from 'firebase-functions';
export const onUserCreate = functions.auth.user().onCreate((user) => {
  // Создать профиль в Firestore
});
``` |
| 5.4 | Развернуть:  
```bash
firebase deploy --only functions
``` |

## 6️⃣ Cloud Storage (для файлов/фоток)
| Шаг | Что делать |
|-----|------------|
| 6.1 | В консоли Firebase → **Storage** → **Get started**. |
| 6.2 | Выбрать регион. |
| 6.3 | Настроить правила безопасности:  
```rules
service firebase.storage {
  match /b/{bucket}/o {
    allow read, write: if request.auth != null;
  }
}
``` |
| 6.4 | В коде загрузки использовать `firebase.storage().ref()` и `.put(file)`. |

## 7️⃣ Аналитика (Google Analytics & Crashlytics)
| Шаг | Что делать |
|-----|------------|
| 7.1 | В консоли Firebase → Analytics → View Reports (если включили в настройках проекта). |
| 7.2 | Включить Crashlytics для отслеживания сбоев:  
```bash
firebase init crashlytics
```
Интегрировать SDK и отправлять события вручную (`firebase.crashlytics().recordError(err)`). |

## 8️⃣ Уведомления (FCM)
| Шаг | Что делать |
|-----|------------|
| 8.1 | В консоли Firebase → Cloud Messaging → Get started. |
| 8.2 | Получить Server key и Sender ID. |
| 8.3 | На клиенте подключить SDK (`firebase.messaging()`) и запросить разрешение:  
```js
import { getMessaging } from "firebase/messaging";
const messaging = getMessaging(app);
await requestPermission();
``` |
| 8.4 | В Cloud Functions отправлять push‑сообщения через `admin.messaging().send()` (если нужно). |

## 9️⃣ Правила безопасности – «Секретный ингредиент»
- **Аутентифицированные пользователи → чтение/запись только своих данных:**
  ```rules
  match /users/{userId} {
    allow read, write: if request.auth.uid == userId;
  }
  ```
- **Общедоступные коллекции** (например, `public_posts`) – чтение открыто, запись ограничена.
- **Storage** → только авторизованные пользователи могут читать/записывать свои файлы.

## 🔑 Итоговый чек‑лист
- [ ] ✅ Создан проект в Firebase и настроен регион.
- [ ] ✅ Подключено приложение (Web / iOS / Android).
- [ ] ✅ Аутентификация включена и провайдеры активированы.
- [ ] ✅ Firestore создан, правила заданы.
- [ ] ✅ Cloud Functions и/или HTTPS‑функции развернуты.
- [ ] ✅ Storage настроен и защищён правилом.
- [ ] ✅ Analytics & Crashlytics подключены.
- [ ] ✅ FCM включено для push‑уведомлений.

## 📌 Полезные ссылки
| Что | Ссылка |
|---|---|
| Firebase Console | https://console.firebase.google.com/ |
| SDK Docs (Web) | https://firebase.google.com/docs/web/setup |
| Firestore Rules | https://firebase.google.com/docs/firestore/security/rules-structure |
| Cloud Functions | https://firebase.google.com/docs/functions/get-started |
| Storage Security | https://firebase.google.com/docs/storage/security |

Теперь у вас есть «живой» шаблон, который можно копировать и вставлять в любой новый проект Firebase. Удачной разработки! 🚀
