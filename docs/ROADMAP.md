# 🗺️ AutoSphere - Дорожная карта развития

Этот документ описывает следующие шаги по улучшению и развитию проекта после завершения основного аудита и исправлений.

## 🎯 Ближайшие задачи (Next Up)

### 1. Реализация динамических виджетов
-   **Проблема:** Виджеты "Тренды недели" и "Топ авторы" на главной странице содержат статичные данные.
-   **Задача:**
    -   Разработать Cloud Function или использовать кешированные запросы для агрегации данных:
        -   **Тренды:** Считать самые популярные теги за последнюю неделю.
        -   **Топ авторы:** Ранжировать пользователей по количеству постов, лайков или подписчиков за период.
    -   Подключить эти данные к соответствующим компонентам на главной странице.

### 2. Завершение функционала админ-панели
-   **Проблема:** Дашборд в `/admin` содержит плейсхолдеры для графиков активности и списка жалоб.
-   **Задача:**
    -   Реализовать сбор и отображение базовой статистики (например, новые пользователи/посты за день).
    -   Создать систему "жалоб" (reports) на посты/комментарии.
    -   Вывести список жалоб на дашборд для модерации.

### 3. Интеграция системы мониторинга ошибок
-   **Проблема:** Отсутствует централизованная система сбора ошибок, возникающих у пользователей в production.
-   **Задача:**
    -   Интегрировать Sentry SDK в проект.
    -   Настроить отправку ошибок из `global-error.tsx`, `error.tsx` и обработчиков `catch`.
    -   Настроить Source Maps для удобной отладки.

## 🚀 Долгосрочные улучшения (Future Enhancements)

### 1. Производительность и SEO
-   **Задача:**
    -   Настроить Vercel Analytics и Speed Insights для отслеживания реальной производительности.
    -   Реализовать `sitemap.xml` и `robots.txt` для улучшения индексации.
    -   Внедрить Incremental Static Regeneration (ISR) для страниц, которые не требуют данных в реальном времени (например, `/about`, `/partners`).

### 2. Расширение функционала
-   **Задача:**
    -   **Real-time чат:** Перевести личные сообщения на real-time обновления с помощью `onSnapshot`.
    -   **Push-уведомления:** Интегрировать Firebase Cloud Messaging (FCM) для отправки push-уведомлений.
    -   **Верификация Email:** Добавить обязательную верификацию email при регистрации.
    -   **OAuth провайдеры:** Реализовать вход через Google, GitHub и другие соцсети.

### 3. Тестирование
-   **Проблема:** Отсутствуют автоматические тесты.
-   **Задача:**
    -   Написать unit-тесты для критически важных утилит и хуков с помощью Jest/Vitest.
    -   Внедрить end-to-end (E2E) тесты для ключевых пользовательских сценариев.
    -   Интегрировать запуск тестов в CI/CD пайплайн.
