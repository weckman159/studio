# 🚀 Инструкция по настройке автоматического деплоя AutoSphere

## Что уже сделано:

✅ Создан `firebase.json` для конфигурации hosting  
✅ Создан `auto-deploy.sh` скрипт для автодеплоя  
✅ Создан GitHub Actions workflow `.github/workflows/firebase-deploy.yml`

## 🔧 Завершить настройку:

### 1️⃣ Получить Firebase токен

Выполните на своей локальной машине (не в Firebase Studio):

```bash
# Установите Firebase CLI (если ещё не установлен)
npm install -g firebase-tools

# Получите CI токен
firebase login:ci
```

Это откроет браузер для авторизации. После входа вы получите **токен** — скопируйте его.

### 2️⃣ Добавить токен в GitHub Secrets

1. Откройте свой GitHub репозиторий
2. Перейдите в: **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Name: `FIREBASE_TOKEN`
5. Value: *вставьте токен из шага 1*
6. Нажмите **Add secret**

### 3️⃣ Отправить изменения в GitHub

```bash
git add .
git commit -m "✨ Добавлена автоматизация Firebase деплоя"
git push origin main
```

## ✨ Как это работает:

🔄 **Автоматический деплой при каждом push**:  
Каждый раз, когда вы делаете `git push`, GitHub Actions автоматически запускает деплой на Firebase Hosting.

⏰ **Периодический деплой каждые 30 минут**:  
GitHub Actions также запускает деплой по расписанию (cron: `*/30 * * * *`).

👍 **Ручной запуск**:  
Вы можете запустить деплой вручную через вкладку **Actions** в GitHub.

---

🎉 **Теперь AutoSphere будет деплоиться автоматически!**
